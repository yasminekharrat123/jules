apiVersion: v1
kind: Service
metadata:
  name: {{ include "db.fullname" . }}
spec:
  selector:
    app: {{ include "db.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
